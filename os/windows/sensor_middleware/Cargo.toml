[workspace]
members = [
    "core_ingress",
    "worker_elastic",
    "worker_splunk",
    "lib_siem_template"
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"