[package]
name = "lib_siem_template"
version = "0.0.1"
edition = "2021"

[dependencies]
tokio      = { version = "1", features = ["full"] }
async-nats = "0.33"
async-trait = "0.1"
tracing    = "0.1"
futures    = "0.3"
# Each worker crate owns its own reqwest (or tiberius) client.