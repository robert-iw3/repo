apiVersion: apps/v1
kind: Deployment
metadata:
  name: linux-sentinel
  namespace: security
  labels:
    app: linux-sentinel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: linux-sentinel
  template:
    metadata:
      labels:
        app: linux-sentinel
    spec:
      containers:
      - name: linux-sentinel
        image: linux-sentinel:latest
        imagePullPolicy: IfNotPresent
        command: ["python3", "/opt/linux-sentinel/linux_sentinel.py"]
        args: ["enhanced"]
        securityContext:
          runAsUser: 65534 # nobody
          runAsGroup: 65534 # nogroup
          capabilities:
            add:
            - NET_ADMIN
            - DAC_READ_SEARCH
            - SYS_PTRACE
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
        ports:
        - containerPort: 8080
          name: api
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        - name: DASHBOARD_PORT
          value: "8080"
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "200m"
            memory: "256Mi"
        volumeMounts:
        - name: log-volume
          mountPath: /var/log/linux-sentinel
        - name: backup-volume
          mountPath: /var/backups/linux-sentinel
        livenessProbe:
          httpGet:
            path: /api/status
            port: api
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/status
            port: api
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: log-volume
        emptyDir: {}
      - name: backup-volume
        emptyDir: {}
      securityContext:
        fsGroup: 65534
      nodeSelector:
        kubernetes.io/os: linux