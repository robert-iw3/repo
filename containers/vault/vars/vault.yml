---
vault_runtime: kubernetes  # Options: docker, podman, kubernetes
vault_namespace: vault
vault_certs_dir: "{{ playbook_dir }}/certs"
vault_config_dir: "{{ playbook_dir }}/config"
vault_compose_file: "{{ playbook_dir }}/docker-compose.yml"
vault_kubernetes_manifest: "{{ playbook_dir }}/vault-deployment.yaml"