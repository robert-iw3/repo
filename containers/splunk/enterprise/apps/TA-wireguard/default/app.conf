# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 7

[launcher]
author = ipfyx
version = 1.1.0
description = This TA app provides field extraction for wireguard debug log. It supports IPv4 and IPv6.

[ui]
is_visible = 0
label = TA for wireguard
docs_section_override = AddOns:released

[package]
id = TA-wireguard

[triggers]
reload.addon_builder = simple
reload.ta_wireguard_account = simple
reload.ta_wireguard_settings = simple
reload.passwords = simple

[author=ipfyx]
email = florian.martin@ipfyx.fr

[id]
name = TA-wireguard
version = 1.1.0

