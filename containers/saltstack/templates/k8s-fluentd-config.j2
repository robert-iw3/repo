apiVersion: v1
kind: ConfigMap
metadata:
  name: fluentd-config
  namespace: {{ namespace }}
data:
  fluent.conf: |
    <source>
      @type forward
      port 24224
      bind 0.0.0.0
    </source>
    <match salt.**>
      @type stdout
    </match>